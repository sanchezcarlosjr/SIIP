type User {
    id: ID!
    name: String!
    email: String
    unit: String
    campus: String
}

input updateUsersInput {
    id: ID!
    name: String!
    email: String
    campus: String
    unit: String
    role_id: ID
}

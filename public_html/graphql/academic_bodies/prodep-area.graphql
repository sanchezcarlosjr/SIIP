extend type Query {
    prodep_area(id: ID @eq): ProdepArea @find
    prodep_areas(
      name_like: String @scope(name: "name")
    ): [ProdepArea]  @paginate(defaultCount: 200)
}

type ProdepArea {
    id: ID!
    name: String
}

extend type Mutation {
    addEmployeesToAcademicBody(data: employeeToAcademicBodyInput @spread): AcademicBody
    removeEmployeesToAcademicBody(data: employeeToAcademicBodyInput @spread): Employee
}

input employeeToAcademicBodyInput {
    id: ID # BUG: employees_id: ID
    academic_body_id: ID
    is_leader: Boolean
    lgac_id: ID
    employees_id: ID
}
